<li class="notif-followed border-bottom mb-1 p-2 rounded-4">
    <div class="d-flex align-items-end" style="margin-bottom: 4px">
      <div class="avatar">
        <%= cl_image_tag post.user.photo.key, class: "avatar"  %>
      </div>
      <div class="d-flex notif text-dark">
        <%= link_to toi_path(post.toi, host: (ENV["DOMAIN"] || "localhost:3000")), class: "text-decoration-none text-dark lh-1 d-flex" do %>
          <strong><p class="me-1" ><%= post.user.username %></p></strong>
          <p class="me-1">a publié sur</p>
          <strong><p class="me-1"><%= truncate(post.toi.title, length: 20) %></p></strong>
        <% end %>
      </div>
    </div>
</li>
